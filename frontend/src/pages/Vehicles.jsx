import { Box, Typography } from '@mui/material';

export default function Vehicles() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Vehicle Management
      </Typography>
      <Typography>
        Manage vehicle information including make, model, plate numbers, and chassis numbers.
      </Typography>
    </Box>
  );
}
