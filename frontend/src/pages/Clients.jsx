import { Box, Typography } from '@mui/material';

export default function Clients() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Client Management
      </Typography>
      <Typography>
        Add, edit, and manage client information with full history tracking.
      </Typography>
    </Box>
  );
}
