import { Box, Typography } from '@mui/material';

export default function Replacements() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Device Replacements
      </Typography>
      <Typography>
        Track all device replacements with reason tracking and automatic inventory updates.
      </Typography>
    </Box>
  );
}
