import { Box, Typography } from '@mui/material';

export default function SIMs() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        SIM Cards Management
      </Typography>
      <Typography>
        Manage SIM card inventory, assignments, and tracking. Similar functionality to Devices page.
      </Typography>
    </Box>
  );
}
