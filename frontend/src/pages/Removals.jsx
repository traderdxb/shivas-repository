import { Box, Typography } from '@mui/material';

export default function Removals() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Device Removals
      </Typography>
      <Typography>
        Track device removals with automatic inventory management based on ownership type.
      </Typography>
    </Box>
  );
}
